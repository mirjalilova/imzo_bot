export TELEGRAM_BOT_TOKEN=8302394017:AAG-_P-z78nzQ7vuywoJGK1FstwyL7Ah8-8


# Imzo-AI API base (no trailing slash)
export IMZO_API_BASE=https://imzo-ai.uzjoylar.uz


# Fixed chat room id to use when sending questions
export IMZO_CHAT_ROOM_ID=e1aae228-975f-4920-837a-a98a4e32968e


# Long-polling config for final response
# How often to poll (seconds)
export POLL_INTERVAL_SECONDS=3
# Max total time to poll (seconds)
export POLL_TIMEOUT_SECONDS=120


# Gateway for final answer retrieval (the service that exposes /get/gpt/responce)
# If your gateway runs locally on the same host as the bot container, replace with the reachable host/IP.
export GATEWAY_BASE=http://localhost:8080


# Authorization header used for GET /get/gpt/responce.
# If blank, the bot will reuse the user's Imzo token.
export GATEWAY_AUTH_BEARER=


# Optional: HTTP client timeout (seconds)
export HTTP_TIMEOUT_SECONDS=20